# shisa-llm-deploy — Python dependencies
# Install with: pip install -r requirements.txt

# vLLM inference engine (installed by setup.sh — not listed here to avoid CPU-only build)
# vllm

# vLLM internal dependencies — required by vllm itself, NOT optional. Do NOT remove.
# fastapi/uvicorn: vLLM's OpenAI API server IS a FastAPI app
# huggingface_hub: vLLM's arg_utils.py imports it; must be <1.0 (transformers pins it)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
huggingface_hub>=0.34.0,<1.0

# Test suite
requests>=2.31.0
psutil>=5.9.0

# Metrics charts (optional — fallback to text if not available)
matplotlib>=3.8.0
Pillow>=10.0.0
