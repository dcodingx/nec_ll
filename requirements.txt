# shisa-llm-deploy — Python dependencies
# Install with: pip install -r requirements.txt

# vLLM inference engine (installed by setup.sh — not listed here to avoid CPU-only build)
# vllm

# Test suite
requests>=2.31.0
psutil>=5.9.0

# Metrics charts (optional — fallback to text if not available)
matplotlib>=3.8.0
Pillow>=10.0.0
