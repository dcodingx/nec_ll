# shisa-llm-deploy — Python dependencies
# Install with: pip install -r requirements.txt

# vLLM inference engine (install separately if GPU build is needed — see setup.sh)
# vllm  # installed by setup.sh via pip, not listed here to avoid CPU-only install issues

# FastAPI wrapper
fastapi>=0.111.0
"uvicorn[standard]>=0.29.0"
httpx>=0.27.0
pydantic>=2.0.0

# Test suite
requests>=2.31.0
psutil>=5.9.0

# Model download (one-time)
huggingface_hub>=0.22.0

# Optional — metrics charts (fallback to pillow if not available)
matplotlib>=3.8.0
Pillow>=10.0.0
